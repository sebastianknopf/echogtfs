services:
  backend:
    ports:
      - "${DEBUG_PORT:-5678}:${DEBUG_PORT:-5678}"
    command:
      [
        "python",
        "-m",
        "debugpy",
        "--listen",
        "0.0.0.0:${DEBUG_PORT:-5678}",
        "--wait-for-client",
        "-m",
        "echogtfs",
      ]
